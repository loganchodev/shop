# ?????? ??
spring:
  application:
    name: shop # ?????? ??
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver # JDBC ???? ????
    url: jdbc:mysql://localhost:3306/shop?serverTimezone=UTC # ?????? URL
    username: webdb # ?????? ??? ??
    password: 12345 # ?????? ????
  jpa:
    properties:
      hibernate:
        show_sql: true # SQL ?? ?? ?? ??
        format_sql: true # SQL ?? ??? ??
    hibernate:
      ddl-auto: create # ?????? ??? ?? ?? ??
    database-platform: org.hibernate.dialect.MySQL8Dialect # ??? SQL Dialect

# ?? ?? ??
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace # Hibernate? ??? SQL? bind ???? ?? ??

# ?? ??
server:
  port: 8094 # ?? ??
